# CodeMate 产品需求初稿

文档版本：v1.1  
最后更新：2026-07-03  
状态：修改稿，待评审

## 一、产品概述

### 1.1 产品定位

CodeMate 是一款面向开发者的 AI 编程助手，围绕“理解项目、辅助编码、修改代码、解释问题”提供对话式开发体验。MVP 阶段优先交付 Web 端和单项目上下文能力，验证核心使用场景和用户价值。

### 1.2 核心价值

- 效率提升：减少重复编码、样板代码和文档编写工作。
- 质量保障：基于上下文提供代码修改、测试生成和风险提示。
- 学习辅助：解释代码逻辑、错误原因和最佳实践。
- 一致性：在项目约束内生成更符合现有风格的代码。

### 1.3 目标用户

| 用户类型 | 特征 | 核心需求 |
| --- | --- | --- |
| 独立开发者 | 全栈开发，时间紧张 | 快速生成样板代码、定位问题、补齐测试 |
| 团队 Leader | 关注代码质量和交付效率 | 代码审查辅助、重构建议、规范一致性 |
| 初级工程师 | 经验不足，学习阶段 | 代码解释、错误分析、最佳实践指导 |
| DevOps 工程师 | 处理脚本和自动化配置 | 生成脚本、解释配置、排查 CI/CD 问题 |

## 二、MVP 范围

### 2.1 MVP 目标

MVP 阶段目标是验证 CodeMate 是否能在真实项目中稳定完成基础编码辅助任务。交付重点包括：

- 支持基于项目文件的对话式问答。
- 支持单文件或少量文件的代码生成、修改和解释。
- 支持 Web 端基础工作台。
- 支持基础任务执行状态、错误提示和使用统计。

### 2.2 MVP 非目标

以下能力不进入 MVP，作为后续版本规划：

- 全量多 IDE 插件同时交付。
- 跨项目长期记忆和个性化学习。
- 企业级私有化部署。
- 自动调度大规模执行集群。
- 完整 SOC2/GDPR 审计体系。
- 自研模型训练或微调平台。

### 2.3 MVP 成功标准

| 指标 | 目标值 | 统计口径 |
| --- | --- | --- |
| 7 日留存 | >= 30% | 完成首个代码任务的注册用户 |
| 代码建议接受率 | >= 40% | 用户点击应用或复制代码视为接受 |
| 首 Token 延迟 | P95 <= 3s | 从用户提交请求到首次流式输出 |
| 任务成功率 | >= 95% | 非用户取消、非模型拒答的正常任务 |
| 用户满意度 | >= 4.0/5 | 任务后评分 |

## 三、功能需求

### 3.1 P0 功能

#### F1 对话式编程

用户可以通过自然语言向 CodeMate 提问、追问和要求修改输出。

| 编号 | 需求 | 验收标准 |
| --- | --- | --- |
| F1.1 | 支持多轮对话 | 同一会话至少保留最近 10 轮上下文 |
| F1.2 | 支持代码解释 | 用户选中文件或代码片段后可获得解释 |
| F1.3 | 支持追问和修正 | 用户可基于上一次回答继续要求调整 |
| F1.4 | 支持中英文输入 | 中英文混合问题可正常理解和回答 |
| F1.5 | 支持流式输出 | 回答生成过程中逐步展示内容 |

#### F2 代码生成

用户可以描述需求，CodeMate 生成可阅读、可复制、可应用的代码。

| 编号 | 需求 | 验收标准 |
| --- | --- | --- |
| F2.1 | 根据自然语言生成代码 | 输出包含代码块和必要说明 |
| F2.2 | 支持主流语言 | MVP 支持 Python、JavaScript、TypeScript |
| F2.3 | 生成单元测试 | 对明确要求的函数或模块生成测试样例 |
| F2.4 | 生成注释和文档 | 支持为函数、类、API 生成说明 |

#### F3 代码修改

用户可以要求 CodeMate 修改现有代码，并查看修改结果。

| 编号 | 需求 | 验收标准 |
| --- | --- | --- |
| F3.1 | 修改指定文件或代码片段 | 输出明确的修改内容和原因 |
| F3.2 | 修复常见错误 | 对语法错误、异常堆栈、测试失败给出修复建议 |
| F3.3 | 小范围重构 | 支持提取函数、简化逻辑、改善命名 |
| F3.4 | 变更预览 | 应用前展示修改摘要 |

#### F4 项目上下文理解

CodeMate 需要读取当前项目的基础上下文，避免脱离项目生成代码。

| 编号 | 需求 | 验收标准 |
| --- | --- | --- |
| F4.1 | 读取项目文件树 | 展示并使用项目目录结构 |
| F4.2 | 读取当前文件内容 | 回答能引用当前文件上下文 |
| F4.3 | 检索相关文件 | 根据用户问题检索少量相关文件 |
| F4.4 | 上下文裁剪 | 超出模型窗口时优先保留当前文件和相关片段 |

#### F5 基础安全与治理

MVP 必须具备基础安全边界，避免泄露和危险执行。

| 编号 | 需求 | 验收标准 |
| --- | --- | --- |
| F5.1 | 敏感信息提示 | 识别疑似密钥、密码并提示用户确认 |
| F5.2 | 请求限流 | 支持用户级基础限流 |
| F5.3 | 操作日志 | 记录用户请求、模型、耗时、错误状态 |
| F5.4 | 错误降级 | 模型或服务失败时返回清晰错误信息 |

### 3.2 P1 功能

| 功能 | 说明 |
| --- | --- |
| VSCode 插件 | 提供侧边栏对话、选区解释和应用修改 |
| 执行沙箱 | 支持受限执行代码片段和测试命令 |
| 多语言扩展 | 增加 Java、Go、Rust 等语言支持 |
| 团队协作 | 支持团队空间、共享提示词和规范检查 |
| PR 辅助 | 生成 PR 描述、变更摘要和审查建议 |

### 3.3 P2 功能

| 功能 | 说明 |
| --- | --- |
| 长期记忆 | 学习用户偏好和项目约定 |
| 私有化部署 | 面向企业客户提供专有部署 |
| 高级审计合规 | 支持完整审计链和合规报告 |
| 多模型智能路由 | 根据任务复杂度和成本选择模型 |

## 四、产品体验

### 4.1 Web 工作台

MVP 使用 Web 工作台作为主入口。

- 左侧：项目文件树。
- 中间：代码查看与编辑区域。
- 右侧：AI 对话面板。
- 底部：任务状态、错误信息和日志摘要。

### 4.2 核心流程

1. 用户打开项目。
2. 系统读取文件树和当前文件。
3. 用户输入问题或选中代码后发起请求。
4. CodeMate 检索相关上下文并生成回答。
5. 用户查看代码建议。
6. 用户复制或应用修改。
7. 系统记录反馈和任务状态。

### 4.3 交互反馈

| 场景 | 反馈方式 |
| --- | --- |
| 开始生成 | 显示加载状态和取消按钮 |
| 生成中 | 流式输出内容 |
| 完成 | 展示复制、应用、重新生成入口 |
| 出错 | 展示错误原因和可执行建议 |
| 上下文不足 | 提示用户选择文件或补充信息 |

## 五、技术方案

### 5.1 总体架构

```text
客户端层
  Web 工作台
      |
API 网关层
  鉴权 / 限流 / 日志 / 路由
      |
核心服务层
  对话服务 / 上下文服务 / 代码修改服务 / 任务服务
      |
基础设施层
  PostgreSQL / Redis / 对象存储 / 向量索引
      |
模型层
  第三方模型 API / 可替换模型适配器
```

### 5.2 核心模块

| 模块 | 职责 | MVP 技术建议 |
| --- | --- | --- |
| 对话服务 | 管理会话、调用模型、流式返回 | FastAPI + SSE |
| 上下文服务 | 文件读取、相关片段检索、上下文裁剪 | 文件索引 + 简单向量检索 |
| 代码修改服务 | 生成修改建议和变更预览 | diff 生成与校验 |
| 任务服务 | 记录任务状态、耗时、错误 | PostgreSQL + Redis |
| 安全服务 | 限流、敏感信息识别、日志脱敏 | 规则引擎 + 中间件 |

### 5.3 数据模型

```python
class Session:
    id: str
    user_id: str
    project_id: str
    created_at: str
    updated_at: str


class Message:
    id: str
    session_id: str
    role: str
    content: str
    context_files: list[str]
    created_at: str
    tokens_used: int


class Task:
    id: str
    user_id: str
    session_id: str
    type: str
    status: str
    error: str | None
    started_at: str
    completed_at: str | None


class Feedback:
    id: str
    message_id: str
    rating: int
    accepted: bool
    comment: str | None
```

### 5.4 上下文策略

MVP 不做复杂长期记忆，采用三层上下文策略：

| 层级 | 内容 | 生命周期 |
| --- | --- | --- |
| 当前上下文 | 当前文件、选中代码、用户输入 | 单次请求 |
| 会话上下文 | 最近 10 轮对话和关键摘要 | 当前会话 |
| 项目上下文 | 文件树、相关文件片段、索引摘要 | 项目周期 |

裁剪优先级：

1. 用户当前输入。
2. 当前文件或选中代码。
3. 最近对话。
4. 检索到的相关文件片段。
5. 项目结构摘要。

## 六、非功能需求

### 6.1 性能

| 指标 | MVP 目标 | 说明 |
| --- | --- | --- |
| 首 Token 延迟 | P95 <= 3s | 不含用户网络异常 |
| 完整响应时间 | P95 <= 30s | 复杂生成任务可更长，但需提示 |
| 并发用户 | 100 | MVP 验证规模 |
| 任务成功率 | >= 95% | 服务端正常返回 |
| 系统可用性 | >= 99.5% | MVP 阶段目标 |

### 6.2 安全

| 编号 | 要求 |
| --- | --- |
| S1 | 所有请求使用 HTTPS |
| S2 | 用户项目数据按租户隔离 |
| S3 | 日志中不明文记录密钥、密码和完整源代码 |
| S4 | 模型请求前进行基础敏感信息检测 |
| S5 | 用户可删除会话和项目索引数据 |

### 6.3 可靠性

| 编号 | 要求 |
| --- | --- |
| R1 | 模型调用失败时支持重试，最多 2 次 |
| R2 | 第三方模型不可用时返回明确错误 |
| R3 | 任务状态可追踪 |
| R4 | 关键配置支持回滚 |
| R5 | 数据每日备份 |

## 七、监控与运营

### 7.1 核心指标

| 指标 | 说明 |
| --- | --- |
| DAU | 每日活跃用户数 |
| 新用户激活率 | 首日完成一次有效代码任务的比例 |
| 代码建议接受率 | 复制或应用代码建议的比例 |
| 任务成功率 | 正常完成的任务占比 |
| 首 Token 延迟 | 模型响应速度 |
| 单次任务成本 | 统计模型调用、检索和执行资源消耗 |
| 用户满意度 | 任务后评分 |

### 7.2 告警规则

| 告警 | 触发条件 | 等级 |
| --- | --- | --- |
| API 错误率过高 | 5 分钟错误率 > 5% | P0 |
| 模型调用失败 | 5 分钟失败率 > 10% | P0 |
| 延迟过高 | P95 首 Token 延迟 > 5s 持续 10 分钟 | P1 |
| 成本异常 | 托管服务日成本超过预算 80% | P2 |

## 八、开源策略与成本说明

CodeMate 定位为开源 Agent 项目。MVP 阶段不设计商业定价，不提供付费套餐，优先验证开源开发者是否愿意安装、使用、反馈和贡献。

### 8.1 开源定位

| 项目 | 策略 |
| --- | --- |
| 代码仓库 | 公开核心代码、文档和示例配置 |
| 许可证 | 待评审，建议在 MIT、Apache-2.0、AGPL-3.0 中选择 |
| 使用方式 | 支持本地运行，用户自行配置模型密钥或本地模型 |
| 社区协作 | 通过 Issue、Discussion、PR 收集需求和贡献 |
| 插件生态 | 后续支持社区贡献工具、模型适配器和工作流 |

### 8.2 Agent 成本说明

开源并不代表运行 Agent 完全免费。成本取决于部署方式和模型选择。

| 成本项 | 是否必然产生 | 说明 |
| --- | --- | --- |
| 大模型 API | 取决于配置 | 使用 OpenAI、Anthropic 等第三方 API 会按 token 计费 |
| 本地模型 | 取决于配置 | 不付 API 费用，但需要本机 GPU/CPU/内存资源 |
| 向量索引 | 可能产生 | 本地存储成本较低，云向量库会产生服务费用 |
| 代码执行沙箱 | 可能产生 | 本地执行主要消耗机器资源，云端执行会有计算成本 |
| 托管服务 | 可能产生 | 如果提供公共 Demo 或云服务，需要服务器、带宽和监控成本 |

MVP 默认推荐两种运行模式：

1. 用户自带模型密钥：项目本身免费，模型调用费用由用户自己的 API 账号承担。
2. 本地模型模式：项目本身免费，用户承担本地硬件资源消耗。

公共托管 Demo 可以保留较小额度，用于体验和演示，但不承诺长期免费高并发使用。

## 九、里程碑

### Phase 1：MVP 验证，8 周

- Web 工作台基础界面。
- 对话式编程。
- 当前文件和项目文件树上下文。
- Python、JavaScript、TypeScript 代码生成。
- 基础日志、限流、错误处理。

### Phase 2：产品化，8 周

- 代码修改预览和应用。
- 基础向量检索。
- VSCode 插件 Alpha。
- 用户反馈闭环。
- 成本看板和基础运营看板。

### Phase 3：团队化，8-12 周

- 团队空间。
- 规范检查。
- PR 辅助。
- 执行沙箱 Beta。
- 多模型适配。

### Phase 4：企业化，持续推进

- 私有化部署。
- 高级审计。
- 权限与合规增强。
- 长期记忆和个性化能力。

## 十、风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 模型输出质量不稳定 | 用户信任下降 | 增加上下文约束、反馈机制和重新生成 |
| 模型成本过高 | 公共 Demo 或托管服务难以长期运行 | 限额、缓存、用户自带密钥和成本看板 |
| 项目上下文不准确 | 生成代码不可用 | 优先当前文件，逐步增强检索能力 |
| 用户代码安全风险 | 数据泄露或合规问题 | 数据隔离、日志脱敏、敏感信息检测 |
| 竞品压力 | 获客困难 | 聚焦轻量工作流和项目上下文体验 |

## 十一、待评审问题

1. MVP 是否只保留 Web 端，VSCode 插件是否推迟到 Phase 2？
2. 首批支持语言是否限定为 Python、JavaScript、TypeScript？
3. 项目上下文是否允许上传完整仓库，还是只读取用户显式选择的文件？
4. 是否需要在 MVP 阶段支持代码执行沙箱？
5. 公共 Demo 是否开放，以及每日模型调用预算由谁维护？

## 十二、附录

### 12.1 参考竞品

- Claude Code
- GitHub Copilot
- Cursor
- Codeium
- Tabnine

### 12.2 术语表

| 术语 | 定义 |
| --- | --- |
| Token | 模型处理文本的基本单位 |
| 上下文窗口 | 模型单次请求可处理的最大文本范围 |
| RAG | 检索增强生成 |
| 沙箱 | 隔离执行代码的受限环境 |
| 首 Token 延迟 | 用户提交请求后收到第一段模型输出的时间 |
